# multiplay_minesweeper2_20260919

## Project
- マルチプレイマインスイーパー。1枚の盤面を全員で協力して開ける
- ルール: 16×16・地雷40。最初に開いたマスとその周り8マスには地雷を置かない。旗・リセット・ほかのプレイヤーのカーソル表示あり
- 言語: Rust。ワークスペースにクレート3つ: `game_core`（ルールとメッセージの型）／`server`／`client`
- ECS は使わず、構造体と関数で書く（ECS を入れる前の版だけが、遊べる状態まで届いたため）

## 設計の約束
- 盤面の正は `server` が持つ。`client` は操作を送り、届いた結果を描く
- 通信は WebSocket＋JSON。`server` は axum で、ページと WebSocket を1つのポートから配る
- 接続先の URL は、ページを配った場所（`location.host`）から組み立てる（旧版は本番の URL を直書きしていて、本番が止まった時点でどこにも繋がらなくなったため）
- 途中から参加した人には、その時点の盤面をまるごと送る
- `game_core` は乱数のシードを外から受け取る。同じシードなら同じ盤面になるので、テストで盤面を再現できる
- 非同期（tokio）は `server` だけで使う。`game_core` は同期で書く（テストを単純に保つため。`client` は WASM で、tokio の一部しか動かないため）

## Code Style
- 命名: モジュール=snake_case / 構造体=PascalCase / 定数=UPPER_SNAKE_CASE
- エラーは `?` で呼び出し元に返す。`server` は `anyhow::Result`、`game_core` は自前のエラー型を使う（呼び出し側がエラーの種類で分岐できるように）
- テスト関数も `Result` を返す形にして、`?` を使う
- 公開APIには `///` ドキュメントコメントを付ける
- `///` に使用例を書く場合は ```rust ブロックで書く。例の中で `?` を使うときは、最後に隠し行 `# Ok::<(), E>(())`（E はその例のエラー型）を置く

## Workflow
- 変更後は `cargo test --workspace` を実行し、doc-tests（出力の「Doc-tests」の欄）も含めて全部合格させる
- `cargo clippy -- -D warnings -D clippy::pedantic -D clippy::nursery` のwarningは修正してからコミット
- `cargo fmt` でフォーマットを統一する

## テスト
- テストはこのプロジェクトの検証役。既存のテストは中身を保ち、通らないときは実装の側を直す

## 止まって聞く場面
次の3つのときだけ、手を動かす前に止まって、理由を添えて確認を求める。それ以外は、テストが通るかどうかで判断して進める。
- 新しい依存クレートを足すとき
- 既存のテストを変える（消す・条件を緩める）とき
- このファイルに書かれた範囲の外の機能を足すとき
